<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.tigerpenguin.lab.bitmapmemory.part3">

    <application android:allowBackup="true"
                 android:icon="@drawable/ic_launcher"
                 android:label="@string/app_name"
                 android:theme="@style/AppTheme">

        <activity android:name=".MainActivity"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".NoHardwareActivity"
                  android:label="No Hardware Acceleration"/>

        <activity android:name=".MaxHardwareActivity"
                  android:label="Maximize Hardware Acceleration"/>

        <activity android:name=".SelectiveHardwareActivity"
                  android:label="Selective Hardware Acceleration"/>

        <activity android:name=".OtherActivity"
                  android:label="Other Activity"/>

    </application>

</manifest>
